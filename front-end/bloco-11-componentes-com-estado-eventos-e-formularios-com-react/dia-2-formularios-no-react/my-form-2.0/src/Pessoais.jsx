import { Component } from "react";

class Pessoais extends Component {
  render() {
    const { nome, email, cpf, endereco, cidade, estado, resumo, descricao, cargo,
      onKeyUpValue, handleChange, removeCaracter, removeFocus, onValueChange, alertMsg, resetButton } = this.props
    return (<div>

    </div>
    )
  }
}

export default Pessoais;
